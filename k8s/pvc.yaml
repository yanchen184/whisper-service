apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-cache
  namespace: whisper
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 10Gi
  volumeName: model-cache-pv  # 指定綁定到哪個 PV
