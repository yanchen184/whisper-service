apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-cache
  namespace: whisper
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
