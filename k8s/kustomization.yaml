apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: whisper

resources:
  - namespace.yaml
  - configmap.yaml
  - pv-nfs.yaml       # NFS 共享儲存。若單台可改 pv-hostpath.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
