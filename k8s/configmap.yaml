apiVersion: v1
kind: ConfigMap
metadata:
  name: whisper-config
  namespace: whisper
data:
  # 模型
  STREAM_MODEL: "base"
  DEVICE: "cpu"
  COMPUTE_TYPE: "int8"

  # 連線
  MAX_CONNECTIONS: "20"
  DEFAULT_LANGUAGE: "zh"

  # 前端 VAD（瀏覽器偵測停頓切段）
  VAD_SILENCE_THRESHOLD: "0.015"
  VAD_SILENCE_DURATION_MS: "800"
  VAD_MIN_SPEECH_MS: "500"
  VAD_MAX_CHUNK_MS: "10000"

  # 後端 VAD（faster-whisper 過濾靜音）
  BACKEND_VAD_ENABLED: "true"
  BACKEND_VAD_THRESHOLD: "0.3"
  BACKEND_VAD_SILENCE_MS: "300"
  BACKEND_VAD_SPEECH_PAD_MS: "200"
